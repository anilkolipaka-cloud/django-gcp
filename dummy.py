import os
from pathlib import Path
from django.core.management.utils import get_random_secret_key

# This is just to test if the dir exists, I don't need to generate a script. I will create html and css files directly.
